{
  "name": "sample-demo",
  "version": "1.0.0",
  "main": "script.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "if not exist build mkdir build && xcopy /E /I /H /Y .\\* .\\build\\ > nul && echo 'Static files are in build/'",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
